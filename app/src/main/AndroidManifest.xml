<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions should be at the TOP level, outside of application -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tholagig"
        tools:ignore="ExtraText">

        <!-- 🔥 ADD THESE FOR GOOGLE SIGN-IN -->
        <meta-data
            android:name="com.google.android.gms.auth.api.phone.REQUEST_NONCE"
            android:value="tholagig_nonce" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- 🔥 ADD GOOGLE SIGN-IN ACTIVITY -->
        <activity
            android:name="com.google.android.gms.auth.api.signin.internal.SignInHubActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <activity
            android:name="com.google.android.gms.common.api.GoogleApiActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- FCM Service must be INSIDE the application tag -->
        <service
            android:name=".messaging.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Your activities -->
        <activity
            android:name=".profile.ClientProfileActivity"
            android:exported="false" />
        <!-- Auth Activities -->
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".auth.UserTypeSelectionActivity"
            android:exported="false" />
        <!-- Dashboard Activities -->
        <activity android:name=".dashboards.FreelancerDashboardActivity" />
        <activity android:name=".dashboards.ClientDashboardActivity" />
        <!-- Job Activities -->
        <activity android:name=".jobs.JobBrowseActivity" />
        <activity android:name=".jobs.JobDetailsActivity" />
        <activity android:name=".jobs.MyJobsActivity" />
        <activity android:name=".jobs.CreateJobActivity" />
        <activity
            android:name=".jobs.MyApplicationsActivity"
            android:exported="false" />
        <activity
            android:name=".jobs.ApplicationDetailsActivity"
            android:exported="false" />
        <activity android:name=".jobs.ApplicationsManagementActivity" />
        <!-- Profile -->
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".messaging.MessagesActivity" />
        <activity
            android:name=".messaging.ConversationsActivity"
            android:exported="false" />
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>