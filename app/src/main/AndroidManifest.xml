<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tholagig">

        <!-- Auth Activities -->
        <activity android:name=".auth.RegisterActivity" android:exported="false" />
        <activity android:name=".auth.LoginActivity" android:exported="false" />
        <activity android:name=".auth.UserTypeSelectionActivity" android:exported="false" />

        <!-- Dashboard Activities -->
        <activity android:name=".dashboards.FreelancerDashboardActivity" />
        <activity android:name=".dashboards.ClientDashboardActivity" />

        <!-- Job Activities -->
        <activity android:name=".jobs.JobBrowseActivity" />
        <activity android:name=".jobs.JobDetailsActivity" />
        <activity android:name=".jobs.MyJobsActivity" />
        <activity android:name=".jobs.CreateJobActivity" />
        <activity android:name=".jobs.MyApplicationsActivity" android:exported="false" />
        <activity android:name=".jobs.ApplicationDetailsActivity" android:exported="false" />
        <activity android:name=".jobs.ApplicationsManagementActivity" />

        <!-- Profile -->
        <activity android:name=".profile.ProfileActivity" />

        <!-- MESSAGES ACTIVITY - 🔥 YOU MUST ADD THIS -->
        <activity android:name=".messaging.MessagesActivity" />

        <!-- Welcome/Launcher -->
        <activity android:name=".WelcomeActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


</manifest>